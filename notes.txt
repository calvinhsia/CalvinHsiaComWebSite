Make the current word centered under the grid.
When won, the congratulations message is put at the bottom and thus invisible on Android unless scrolled to.
The Settings button: when clicked, it shows on top and not visible on Android. Make it show between the grid and the circle. When settings saved, automatically create a new game.
Make the circle 25% bigger, font and size.
Make the grid letter font larger, retaining the size of the grid cell, so easier to see.
The score textboxes (hint/score and word counts) are too small to read on Android. Make the font larger.
The Found words list has a scrollbar, which is redundant in narrow view. 
When going to the home page, then back to the game page, the game state is lost. Can the game state be persisted?
Ensure that the game state is saved when navigating away and restored upon returning.

click/touch on current word should go do dictionary url.